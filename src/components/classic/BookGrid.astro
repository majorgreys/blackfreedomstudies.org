---
import { Image } from 'astro:assets';

interface Props {
  books: ImageMetadata[];
}

const { books } = Astro.props;
---
{books.length > 0 && (
  <div class="c-book-grid">
    {books.map((book) => (
      <Image src={book} alt="Book cover" width={100} />
    ))}
  </div>
)}
