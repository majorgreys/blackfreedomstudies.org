---
import BookCover from '../shared/BookCover.astro';

interface Props {
  books: Array<ImageMetadata | string>;
}

const { books } = Astro.props;
---
{books.length > 0 && (
  <div class="r-book-grid">
    {books.map((book) => (
      <BookCover src={book} width={280} />
    ))}
  </div>
)}
