---
name: feedback_uv_run
description: User prefers uv run over bare python3 for running Python scripts
type: feedback
---

Use `uv run` instead of `python3` to execute Python scripts.

**Why:** User preference for uv as their Python runner.

**How to apply:** Always use `uv run script.py` instead of `python3 script.py` when running Python scripts.
